{% comment %}
Renderizo el componente 'marquee' como un elemento independiente.
Este puede incluir contenido animado adicional en otras secciones.
{% endcomment %}
{% render 'marquee' %}

{% comment %}
Barra superior de notificaciones.
Incluye un mensaje informativo y un ícono animado gestionado por el script.
{% endcomment %}
<div class="top-bar">

  {% comment %}
  Contenedor principal para la barra superior.
  - Organiza el contenido de la barra en una estructura centralizada.
  {% endcomment %}
  <div class="top-bar__container">

    {% comment %}
    Contenido de la barra superior.
    Este elemento contiene el texto y el ícono, y es administrado por el script para generar la animación.
    {% endcomment %}
    <div class="top-bar__content" id="topBarContent">
      
      {% comment %}
      Texto y elemento decorativo de la barra.
      Incluye un ícono dinámico y un mensaje informativo.
      {% endcomment %}
      <span class="top-bar__text">

        {% comment %}
        Ícono decorativo de la barra superior.
        Renderizado dinámicamente mediante un snippet reutilizable.
        {% endcomment %}
        <div class="top-bar__icon">
          {% render 'icons/diamonds-marquee' %}
        </div>

        {% comment %}
        Mensaje dinámico de la barra superior.
        Comunica información clave como ofertas o políticas de envío.
        {% endcomment %}
        <p>FREE SHIPPING FOR ORDERS OVER $100</p>
      </span>
    </div>
  </div>
</div>

<script>
  /* 
  Inicializa la funcionalidad de la barra animada (marquee).
  - `marqueeElement`: Elemento que contiene el contenido animado.
  - Configuración:
      - `speed`: Velocidad del desplazamiento del marquee (20 unidades).
      - `padding`: Espaciado entre los elementos del marquee (20 unidades).
  */
  const marqueeElement = document.getElementById('topBarContent');
  
  const marquee = new MarqueeManager(marqueeElement, {
    speed: 20, // Velocidad del desplazamiento del contenido.
    padding: 20, // Espaciado entre elementos en el marquee.
  });
</script>
